"""Engine adapters. Each adapter wraps one execution engine + one configuration.

The trio for v0.1: `df_engine.DeltaForgeEngine`,
`spark_default_engine.SparkDefaultEngine`, `spark_tuned_engine.SparkTunedEngine`.
"""
